# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=stds
DB_PASSWORD=stds
DB_NAME=stds_dev
DB_SSLMODE=disable

# 完整 DSN（golang-migrate 使用）
# DATABASE_URL=postgres://stds:stds@localhost:5432/stds_dev?sslmode=disable
